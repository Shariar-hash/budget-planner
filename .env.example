# MongoDB Atlas Connection (REQUIRED)
# Replace with your actual MongoDB Atlas connection string
# Format: mongodb+srv://[username]:[password]@[cluster].[subdomain].mongodb.net/[database]?retryWrites=true&w=majority
REACT_APP_MONGODB_URI=REPLACE_WITH_YOUR_MONGODB_ATLAS_CONNECTION_STRING

# JWT Secret (REQUIRED - Generate a strong secret for production)
# Use a long, random string for security (minimum 32 characters)
JWT_SECRET=REPLACE_WITH_STRONG_SECRET_32_CHARS_MINIMUM

# API Configuration
REACT_APP_API_URL=http://localhost:5000/api

# App Configuration
REACT_APP_NAME=Budget Planner
REACT_APP_VERSION=1.0.0

# Production Example:
# REACT_APP_API_URL=https://yourdomain.com/api
# JWT_SECRET=example_use_crypto_random_generator_for_real_secret
